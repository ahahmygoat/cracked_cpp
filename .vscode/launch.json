{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run main (Windows)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/main.exe",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Build (GCC) Windows"
    },
    {
      "name": "Run main (macOS/Linux)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/main",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Build (GCC) macOS/Linux"
    }
  ]
}
